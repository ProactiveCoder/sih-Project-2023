import React from 'react';
import './Alert.css'; // Import your CSS for styling

function MovingText() {
  return (
    <div className="moving-text-container">
        <h2 className='altxt'>Alert</h2>
      <div className="moving-text">React's built-in useState Hook and the setTimeout function. This method allows you to have full control of the animation. To get started, you'll first need to create a component called Typewriter.</div>
    </div>
  );
}

export default MovingText;
